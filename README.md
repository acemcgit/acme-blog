Please run for dependencies:
composer install
npm install

run to start server:
php artisan serve - (for laravel)
npm run dev - (for vite)

and I also have migration script here.
just run php artisan migrate


